name: Build Encosian Keyboard

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build placeholder
        run: echo "Keyman build handled locally"
